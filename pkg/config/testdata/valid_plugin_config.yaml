version: 1
profile: default
profiles:
  default:
    restapi:
      endpoint: https://eu-west-rbx.okms.ovh.net
      auth:
        type: token
        okms-id: okms_id
        token: token
    sigstore-kms-ovhcloud:
      on-key-conflict:
        strategy: use-more-recent
        max-keys-to-try: 3
